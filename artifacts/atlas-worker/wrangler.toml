name = "atlas-api"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# Non-secret configuration — safe to commit
[vars]
SUPABASE_URL = "https://unfddiecgfyzpincussj.supabase.co"
PORTAL_BASE_URL = "https://atlas.mach7technologies.com"

# ── Secrets ───────────────────────────────────────────────────────────────────
# Set each with `wrangler secret put <KEY>` — do NOT commit actual values.
#
# wrangler secret put SUPABASE_SERVICE_ROLE_KEY
# wrangler secret put SUPABASE_ANON_KEY
# wrangler secret put SUPABASE_JWT_SECRET
# wrangler secret put RESEND_EMAIL_KEY
# wrangler secret put RECAPTCHA_SECRET_KEY
